#[derive(Debug, Clone)]
pub struct CompositionMatch {
    pub match_id: i32,
    pub club_id: i32,
    pub note_generale: f32,
    pub note_collectif: f32,
    pub forme_generale: f32,
    pub finition: f32,
}